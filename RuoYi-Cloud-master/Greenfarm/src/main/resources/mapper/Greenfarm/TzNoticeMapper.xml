<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.Greenfarm.mapper.TzNoticeMapper">
    
    <resultMap type="TzNotice" id="TzNoticeResult">
        <result property="id"    column="id"    />
        <result property="shopId"    column="shop_id"    />
        <result property="title"    column="title"    />
        <result property="content"    column="content"    />
        <result property="status"    column="status"    />
        <result property="isTop"    column="is_top"    />
        <result property="publishTime"    column="publish_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectTzNoticeVo">
        select id, shop_id, title, content, status, is_top, publish_time, update_time from tz_notice
    </sql>

    <select id="selectTzNoticeList" parameterType="TzNotice" resultMap="TzNoticeResult">
        <include refid="selectTzNoticeVo"/>
        <where>  
            <if test="shopId != null "> and shop_id = #{shopId}</if>
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="isTop != null "> and is_top = #{isTop}</if>
            <if test="publishTime != null "> and publish_time = #{publishTime}</if>
        </where>
    </select>
    
    <select id="selectTzNoticeById" parameterType="Long" resultMap="TzNoticeResult">
        <include refid="selectTzNoticeVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertTzNotice" parameterType="TzNotice" useGeneratedKeys="true" keyProperty="id">
        insert into tz_notice
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="shopId != null">shop_id,</if>
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
            <if test="status != null">status,</if>
            <if test="isTop != null">is_top,</if>
            <if test="publishTime != null">publish_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="shopId != null">#{shopId},</if>
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
            <if test="status != null">#{status},</if>
            <if test="isTop != null">#{isTop},</if>
            <if test="publishTime != null">#{publishTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateTzNotice" parameterType="TzNotice">
        update tz_notice
        <trim prefix="SET" suffixOverrides=",">
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isTop != null">is_top = #{isTop},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteTzNoticeById" parameterType="Long">
        delete from tz_notice where id = #{id}
    </delete>

    <delete id="deleteTzNoticeByIds" parameterType="String">
        delete from tz_notice where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>